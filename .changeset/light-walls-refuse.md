---
"packlet": patch
"@packlet/cli": patch
---

Updates the CLI entry point logic for both the cli and packlet packages to improve compatibility with CommonJS (CJS) and ECMAScript Module (ESM) environments. Also ensures that the bin directory is included in the published package files.
